---
Name: gateway-example
---

# Copy to your mysite/_config/ folder and customise with your Moneris credentials. 
# Highly recommended to not add the gateway config to your 
# source control / repository, as the credentials need to be kept secured.

#GatewayInfo:
#  Moneris:
#    use_authorize: false
#    use_static_route: true
#    parameters:
#      store_id: '[dev store_id]'
#      api_token: '[dev api_token]'
#      checkout_id: '[dev checkout_id]'
#      environment: 'qa'
#    required_fields:
#      - 'store_id'
#      - 'api_token'
#      - 'checkout_id'
#      - 'environment' 

---
Only:
  environment: 'live'
---
# Supply different credentials for "live" environment.
#SilverStripe\Omnipay\GatewayInfo:
#  Moneris:
#    use_authorize: false
#    use_static_route: true
#    parameters:
#      store_id: '[prod store_id]'
#      api_token: '[prod api_token]'
#      checkout_id: '[prod checkout_id]'
#      environment: 'prod'
#    required_fields:
#      - 'store_id'
#      - 'api_token'
#      - 'checkout_id'
#      - 'environment' 